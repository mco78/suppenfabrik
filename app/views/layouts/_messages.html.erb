<div class="row">
	<div class="twelve columns">
		<% flash.each do |name, msg| %>
			<% if msg.is_a?(String) %>
				<div class="alert-box {name}" id="messages">
		    		<%= msg %>
		    		<a href="" class="close">&times;</a>
	        	</div>
			<% end %>
		<% end %>
	</div>
</div>

