<h1>User bearbeiten</h1>

<%= form_for @user do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email %>
  </div>	

  <%= render "rights_form", :f => f %>


  <br>
  <br>
  <div class="actions">
    <%= f.submit "speichern", :class => "medium success button" %>
  </div>
<% end %>

<p><%= link_to 'Passwort ändern', edit_password_user_path(@user), :class => "small round button" %></p>

<%= link_to 'zurück', users_path, :class => "tiny round secondary button" %>