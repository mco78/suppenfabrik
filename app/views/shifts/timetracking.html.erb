<div class="row">
  <div class="twelve columns">
    <h2>Zeiterfassung</h2>
  </div>
  <div class="nine columns">
    <p>Hier könnte noch ein kleiner Erklärtext für die Mitarbeiter stehen, damit sich neue Mitarbeiter nicht verloren vorkommen. Vielliecht kann man ihn auch weglassen. Viel Spaß beim Arbeiten!</p>
    <div class="row" id="start-stop">
      <div class="two columns"></div>
      
      <div class="three columns" id="start">
        <% if @current_shift.nil? %>
          <%= link_to 'Start', { :action => :start }, {:class => "xlarge success button"} %>
        <% else %>
          <a href="stop.html" class="disabled xlarge success button">Start</a>
        <% end %>
      </div>

      <div class="two columns"></div> 
      <div class="three columns" id="stop">
        <% if @current_shift.nil? %>
          <a href="#" class="disabled xlarge alert button">Stop</a>
        <% else %>
          <%= link_to 'Stop', stop_shift_path(@current_shift), {:class => "xlarge alert button"} %>
        <% end %>

        <p id="button-info">Nach dem Klick werden 5 Minuten zusätzlich gutgeschrieben</p>
      </div>
      <div class="two columns"></div> 
      
    </div>

    <div class="panel" id="zusatzinfos">
      <h5>Info:</h5>
      <% if @current_shift.nil? %>
        <p>Letzte Schicht: <%= @last_shift.start.strftime("%d.%m.%y %H:%M") %> bis <%= @last_shift.stop.strftime("%d.%m.%y %H:%M") %></p>
      <% else %>
        <p>Aktuelle Schicht hat um <%= @current_shift.start.strftime("%d.%m.%y %H:%M") %> begonnen.</p>
      <% end %>

    </div>
  </div>

  <div class="three columns">
    <div class="panel" id="userinfos">
      <h6>Benutzer:</h6>
      <p><%= @user.name %></p>
      <h6>angemeldet seit:</h6>
      <p><%= @user.current_sign_in_at.strftime("%d.%m.%y %H:%M") %></p>
      <p><a href="#">abmelden</a></p> 
    </div>
  </div>
</div>